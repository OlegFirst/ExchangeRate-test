import { selectorComponentIdValues } from '../../common/interfaces';

export const selectorComponentsCount: selectorComponentIdValues = 2;

export const ids: any = Array(selectorComponentsCount).fill(0).map((_, index) => index + 1);